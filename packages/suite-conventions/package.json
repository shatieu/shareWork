{
  "name": "suite-conventions",
  "version": "0.1.0",
  "description": "Shared Ship-suite conventions: ~/.suite/services.json discovery helpers, hook-event shapes, the typed station-plugin contract, VoyageItem, and local-daemon security helpers. Internal workspace name only -- npm naming/publishing is a Captain decision (plan 03 §9).",
  "type": "module",
  "private": true,
  "license": "MIT",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@eslint/js": "^9.19.0",
    "@types/node": "^22.10.7",
    "eslint": "^9.19.0",
    "fastify": "^5.9.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.21.0",
    "vitest": "^3.0.4"
  }
}
