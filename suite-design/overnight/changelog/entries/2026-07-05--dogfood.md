---
id: dogfood-chart-room-onto-sharework-itself
---

# Housekeeping — Chart Room dogfooded onto shareWork itself

Package 01-housekeeping-dogfood (deferred step from MORNING-REPORT §4.2), on `ship-wave1-dogfood`:

- **`.chartroomignore` support** (new product behavior, FO-approved): `discoverDocFiles` now also
  honors a repo-root `.chartroomignore` (gitignore syntax, additive with `.gitignore`, same
  `ignore` matcher instance, zero new dependencies). Every consumer — `init`/`index`/`check`/
  `fix-links`, the pre-commit hook, the daemon — inherits it, because they all funnel through
  discovery. Tests in `packages/chartroom/test/chartroomignore.test.ts`; README subsection added.
- **Root `.chartroomignore`** scoping this repo: `team-tasks/` (untouchable), starter/template
  content, byte-exact editor fixtures, `.claude/`, the Lookout, kickoff prompts.
- **`chartroom init --no-hook` run against shareWork**: 49+1 ids assigned (every managed doc —
  specs, overnight tracking, changelog entries, root docs, package READMEs, new root `CLAUDE.md`);
  audited as exact 4-line frontmatter prepends only. `chartroom check`: clean, exit 0.
- **Agent surfaces**: `chart-room` skill installed at `.claude/skills/chart-room/SKILL.md`; root
  `CLAUDE.md` Chart Room section added. The `PostToolUseFailure` agent hook install was blocked by
  the session permission system (agent self-modification of `.claude/settings.json`) — parked as a
  one-line human step in CAPTAIN-TODO.md, alongside the deliberately deferred post-merge
  `.git/hooks/pre-commit` install.
- **Acceptance** (`packages/chartroom/acceptance/dogfood-sharework.mjs`): a `git mv` of a
  suite-design doc self-heals via `chartroom resolve` and the raw index; the changelog directory
  renders through the daemon's real docs routes.
