---
id: 2026-07-06-comm-phase-1-package-13
---

# 2026-07-06 — Comm phase 1 (package 13)

**The Comm's laptop half is aboard (VoiceBridge_Spec §9.1, text mode).** New `ship-voice`
station mounted into `ship serve`: the full §3 voice toolset over local HTTP
(`/api/ship-voice/fleet_status`, `session_status`, `send_to_session`, `dispatch`, `approve`,
`deny`, `ledger_add`, `ledger_status`, `whats_new`), every response carrying a `spoken`
string from the new summarize-for-speech layer (§4: names not ids, counts as words, rounded
numbers, lists capped at 3, long content summarized by Haiku with a deterministic fallback).

- Fleet read: `claude agents --json` (verified live) behind an injected `FleetSource`;
  send/dispatch via detached `claude -p --resume` / `claude -p` behind `FleetControl`.
- §6 approval rails: two-step read-back before executing, exact confirm phrases for
  destructive commands, always-allow by voice rejected at the schema.
- §3 payload minimization enforced and tested: summaries and command metadata only — never
  file contents, paths, or diffs.
- 73 unit tests + a 30-check acceptance script driving the real `ship` bin with deterministic
  seams; live proofs: real-fleet `fleet_status` paragraph + one budget-capped Haiku speech
  summary. ElevenLabs (phase 2+) parked as a CAPTAIN-TODO seam — no credentialed code faked.
