---
id: decisions-deferred-to-the-captain
---

# Decisions deferred to the Captain

Format per entry: what, why it's deferred, default taken (if any), how to override.

## Feature branch naming: dashes instead of slashes

- **What:** kickoff prompt specifies feature branches as `ship-wave1/cr-phase-1` (slash-nested under the integration branch name).
- **Why deferred:** git refuses to create a branch nested under an existing branch name (`ship-wave1/scaffold` collides with existing ref `ship-wave1` — refs/heads is a filesystem-like namespace, a name can't be both a file and a directory).
- **Default taken:** feature branches use dashes instead: `ship-wave1-scaffold`, `ship-wave1-cr-phase-1`, `ship-wave1-cr-phase-2`, etc. Same intent (one branch per package, merges into `ship-wave1`), trivially reversible (rename before pushing if the Captain prefers slashes with a differently-named integration branch, e.g. `wave1` instead of `ship-wave1`).
- **Review tomorrow:** confirm naming convention, rename if desired before further work stacks on top.

## Package 1 (Chart Room phase 1) plan review — decisions taken by First Officer

Plan: `suite-design/overnight/plans/01-cr-phase1-plan.md`. Team Lead flagged 4 items in its §12; reviewed and resolved as follows so implementation isn't blocked:

- **Package name `"chartroom"` (unscoped).** Not treated as a guess — it's spec-literal (`ChartRoom_Spec.md` §7/§9 explicitly say `npx chartroom serve`). Approved as-is. Nothing publishes tonight regardless.
- **New dependencies** (`gray-matter`, `unified`, `remark-parse`, `remark-gfm`, `remark-frontmatter`, `unist-util-visit`, `commander`, `ignore` as runtime; `vitest`, `@types/node`, `eslint`, `typescript-eslint` as dev). Ondřej's global rule is "never add dependencies without asking" — but this kickoff prompt is that ask: `ChartRoom_Spec.md` §7 names the remark/unified family explicitly as the stack, and the mission's own Definition of DONE requires working build/lint/test, which requires dev tooling. `gray-matter`, `commander`, and `ignore` aren't spec-named but are small, MIT/permissive, zero-network, zero-telemetry, and trivially removable/replaceable — approved as the conservative low-risk default. **Review tomorrow:** confirm this reasoning holds; swap any of the three non-spec-named packages if you'd rather they were hand-rolled.
- **Monorepo pre-commit hook composability with `team-tasks/`.** Confirmed by direct check: no `.git/hooks/pre-commit` exists yet and `team-tasks/` has no husky/pre-commit config, so there's nothing to collide with tonight. Approved the plan's "refuse to clobber + print manual chaining instructions" approach; no chaining framework built. **Review tomorrow** if `team-tasks/` ever wants its own hook.
- **`fix-links` default write behavior — overridden, not deferred.** Plan proposed `--write` as the implicit default (bare `chartroom fix-links` mutates files). Changed this before implementation: bare `chartroom fix-links` now defaults to the same behavior as `--dry-run` (report only); an explicit `--write` flag is required to mutate. Matches the industry convention (`eslint --fix`, `prettier --write` both require an explicit flag) and the mission's general safety-first posture around file mutation. This is a First Officer correction to the plan, not a Captain-level open question — mentioning here for visibility only.

## Chart Room phase 1 (Developer stage): `.docs/` not yet in any `.gitignore`

- **What:** the index schema (plan §4) and this repo's own `packages/chartroom/src/repo.ts` both
  treat `.docs/index.json` as "gitignored, per-repo" -- `repo.ts`'s doc-discovery walk already
  skips a built-in `.docs` directory name so `chartroom index` never indexes its own output, but
  no actual `.gitignore` entry for `.docs/` exists yet anywhere in this monorepo (checked the root
  `.gitignore` and found no `packages/chartroom/.gitignore` either).
- **Why deferred:** adding a `.gitignore` entry wasn't in the Developer stage's assigned file list
  (plan §3), and this Developer session never ran `chartroom init`/`chartroom index` against the
  real repo tree (only against disposable scratch dirs, per instructions) -- so nothing has
  actually landed in the real working tree as an untracked `.docs/index.json` yet. No urgency, but
  worth fixing before anyone runs `chartroom init` for real in this repo.
- **Default taken:** left as-is; not silently patched by the Developer since touching
  `.gitignore` (root or a new package-scoped one) is a small but real repo-wide change outside this
  package's assigned scope.
- **Review tomorrow:** add `.docs/` to the root `.gitignore` (or a `packages/chartroom/.gitignore`)
  before the first real `chartroom init` run in this repo, so `.docs/index.json` doesn't show up as
  an untracked file / risk getting committed by accident.
- **Resolved same night:** First Officer added `.docs/` to the root `.gitignore` (`4bb3688`) before
  merging phase 1 — no longer open, kept here for the record.

## Package 2 (Chart Room phase 2) plan review — decisions taken by First Officer

Plan: `suite-design/overnight/plans/02-cr-phase2-plan.md`. Team Lead flagged 8 items in its §11; reviewed and resolved as follows so implementation isn't blocked:

- **Two-package split (`packages/chartroom` gains the daemon, new `packages/chartroom-ui` is the React/Vite frontend, its build copied into `chartroom`'s own `dist/public`).** Approved as recommended — matches how CLI-with-embedded-webview tools are commonly structured, keeps `npx chartroom serve` self-contained after publish, needs zero workspace-glob changes. Sets the pattern phases 3-5 will likely extend; flagged for Captain review, not blocking.
- **New dependencies** (`fastify`, `@fastify/static`, `chokidar`, `remark-directive`, `remark-directive-rehype` for the daemon; `react`, `react-dom`, `react-markdown`, `remark-gfm`, `remark-directive`, `remark-directive-rehype`, `rehype-slug` for the UI; `vite`, `@vitejs/plugin-react`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom` as dev). Same reasoning as phase 1: `ChartRoom_Spec.md` §7 names Fastify/chokidar/unified-remark/React+Vite explicitly as the stack; the rest is DoD-required test/build tooling. All verified live against npm today by the Team Lead (not recalled from training data). Approved as proposed, including the **`chokidar ^4.0.3` pin over the newer `^5.0.0`** specifically to avoid silently bumping the repo's `engines.node` floor from `>=20` to `>=20.19` — the safer, lower-risk pick. **Review tomorrow:** confirm this reasoning holds; swap to chokidar v5 + bump engines repo-wide if preferred.
- **Staleness scope — narrow reading approved.** Spec §6/§10 loosely tag "staleness (dashboard)" as phase-2-ish, but the Build Order's literal §8 item 2 acceptance line only asks for "missing-link tombstone display." Approved: phase 2 implements only tombstone display (direct reuse of phase 1's `check.ts`), no `ttl_days`/`sources:` freshness gates or orphan detection. This is a First Officer scope-reading call, not a Captain-level question — matches the precedent of phase 1's fix-links default override (resolving plan ambiguity rather than guessing bigger scope).
- **Mermaid diagram rendering — deferred**, not built this phase. Mentioned in spec §3's general description, absent from the Build Order's literal phase-2 line; cheap to add later as an isolated `components: { code: ... }` override with no architectural knock-on effects.
- **No real-browser smoke test / Playwright — resolved, not deferred.** Rather than adding Playwright as a new dependency for one phase, the First Officer will do a manual real-browser QA pass using the `claude-in-chrome` browser tooling already available in this environment, as part of the adversarial Reviewer step for this package. Approved: automated `.inject()` + jsdom/RTL tests prove the data+render logic; the manual pass proves the actual built bundle boots in a real browser. No new dependency needed.
  - **Actually attempted, could not complete:** the Chrome extension was not connected in this session (`tabs_context_mcp` returned "Browser extension is not connected"), so no real click-through was possible tonight. Substituted the next-best structural proxy: built both packages for real, registered two throwaway repos (sandboxed `HOME`, real `~/.chartroom/repos.json` never touched), created a real tombstone, booted `chartroom serve` in the background, and hit it with live `curl` — confirmed `GET /` returns the real built `index.html` referencing an asset file that actually exists on disk, and `GET /api/repos/:id/docs/:docId` returns the enriched `brokenLinks` (with `deletedAt`) for the tombstoned link. This proves the server/data half end-to-end; it does **not** prove the React app actually renders/hydrates correctly in a live browser (hash routing, TOC scroll-into-view, `:::llm` collapse toggle, dark mode). **Review tomorrow:** do the actual browser click-through once the Chrome extension is connected — specifically switch repos, open the tombstoned-link doc, expand/collapse an `:::llm` block and a section, click a TOC entry.
- **`chartroom register`/no `unregister`.** Approved as minimal — CLI-only registration, no `unregister` command this phase. Trivial to add later if wanted.
- Nothing needing `REMOVALS.md` or touching `team-tasks/` — confirmed by the Team Lead's own design, not by omission (re-verified by First Officer against the plan's actual file list, §3).

## Package 3 (Chart Room phase 3, editor) plan review — decisions taken by First Officer

Plan: `suite-design/overnight/plans/03-cr-phase3-plan.md`. Team Lead flagged 10 items in its §11; reviewed and resolved as follows so implementation isn't blocked. This is the highest-risk phase so far (Milkdown WYSIWYG + a hard byte-identical round-trip requirement the spec itself names as a known failure mode) — resolutions below lean on the Team Lead's own research, which was unusually thorough and honest about what it couldn't confirm from Milkdown's (client-rendered, unfetchable) docs site.

- **Round-trip strategy — approved as designed.** Block-level diff-and-splice against the original raw text (mirrors phase 1's `fix-links.ts` byte-offset discipline), with a strict-allowlist opaque-passthrough node for anything Milkdown's presets don't understand (`html`, `remark-directive` blocks, and defensively any unrecognized node type). The Team Lead's research confirmed, concretely, that whole-document Milkdown/`remark-stringify` serialization would (a) rewrite bullet/heading style on essentially every real pre-existing doc and (b) actively corrupt `:::llm`/`:::human`/`:::ask-me` directive blocks since Milkdown's default presets can't parse `remark-directive` syntax at all — this is a real, confirmed finding, not a hypothetical, and the block-diff design is the right response to it. Approved.
- **New dependencies**: `@milkdown/kit` (^7.21.2, not `@milkdown/crepe` — the composable-primitives package is the right pick given the customization this phase needs), `@milkdown/react` (^7.21.2), `fuse.js` (^7.4.2) as new `chartroom-ui` runtime deps; `remark-frontmatter` (^5.0.0) newly added to `chartroom-ui` (already a phase-1 dep, same version). Approved — Milkdown is spec-named (§7), the rest is small/MIT/zero-network per the established approval pattern. `cmdk` and a dedicated diffing library both correctly rejected as unnecessary per the project's "don't add a dependency for ~50-100 lines" precedent.
- **Cross-package boundary: exporting phase-1's offset-walking primitives for `chartroom-ui` to import.** Approved — this is the one place the plan deviates from phase 2's "duplicate rather than touch phase 1/2 files" precedent, but for a good, narrow reason: byte-offset extraction across 7+ node types is correctness-critical for the round-trip guarantee, and phase 1 already had one documented empirical false-start getting this logic right (phase 1 plan §10 risk #3) — reusing the tested implementation is safer than re-deriving it under phase-3 time pressure. Additive only (new export surface, zero behavior change to any existing export).
- **`fix-links.ts` behavior extension for image-href repair.** Approved — the one actual behavior change (not just an export addition) to existing phase-1 code, needed to make "pasted image self-heals after `git mv`" literally true rather than true by accident. Narrow, additive, test-covered per the plan's own §9.2 acceptance script.
- **Asset folder semantics (`assets/<doc-id>/`, repo-root-relative).** Approved as the Team Lead's recommended reading of the spec's ambiguous "configurable folder" wording — matches the spec's literal example path shape most directly.
- **Explicit Save (button + Ctrl+S), not autosave.** Approved — matches the project's established "never surprise the git diff" ethos better than a debounced autosave that would write mid-thought edits to disk.
- **Hand-rolled Ctrl+K modal, not `cmdk`.** Approved for this phase; `cmdk` remains a reasonable upgrade if phase 4/5 wants a broader command-palette feel later.
- **No automated real-browser smoke test for the editor.** Same resolution as phase 2 — First Officer will attempt a manual `claude-in-chrome` QA pass during review; if the Chrome extension is unavailable again (as it was for phase 2), substitute the same daemon-boot + live-curl/API proxy plus the automated headless round-trip test suite as the primary evidence, and log the gap honestly rather than claim full browser QA was done.
- Nothing needing `REMOVALS.md` or touching `team-tasks/` — confirmed by the Team Lead's own design.

## Package 3 (Chart Room phase 3, editor) — Developer-stage findings and deviations from the approved plan

Implemented on `ship-wave1-cr-phase-3`. The plan's own §1.7 honesty flag ("Milkdown's exact API surface... could not be confirmed... a real, named gap") was taken seriously: a first spike installed `@milkdown/kit`+`@milkdown/react` and read the installed `.d.ts`/`.js` files directly before writing any real code. Findings below are what actually changed versus the approved plan, plus a build-graph problem the plan's own approved cross-package-export decision (item 3 above) turned out to require fixing.

- **Milkdown API spike: mostly confirmed, one real design-invalidating conflict found.** `useEditor`/`<Milkdown/>`/`MilkdownProvider`, `Editor.make()`/`rootCtx`/`defaultValueCtx`, `listener`/`listenerCtx`, `$node`, `$remark`, `parserCtx`/`serializerCtx`/`schemaCtx`, `commonmark`/`gfm` preset arrays all matched the plan's assumed shapes exactly. One real divergence: `@milkdown/preset-commonmark` already registers its own node schema for mdast type `'html'` — but as an **inline** atom (`group: 'inline'`, for a `<span>` mid-paragraph), not a block-level one. The plan's literal §3.1 step 4 design ("build the Milkdown document by concatenating... a custom node for each opaque block") sidesteps this cleanly by never feeding whole raw text (with directive/HTML syntax in it) through Milkdown's own markdown-string parser at all — prose blocks are parsed via `parserCtx` on their own isolated text; opaque blocks are constructed directly as `chartroomOpaqueBlock` node instances. An earlier, since-abandoned implementation attempt (feeding the whole body through Milkdown's parser with `$remark(remarkDirective)` wired in) hit this conflict directly; reverting to the plan's literal per-block-construction design resolved it. Documented in `opaqueNode.ts`'s file header.
- **A real circular-workspace-dependency risk in the approved cross-package export (item 3 above), found and fixed.** Making `chartroom-ui` depend on `chartroom` (for the `./markdown` subpath export) while `chartroom` already depended on `chartroom-ui` (devDependency, purely to order the UI-bundle-copy step in its own build script) created a genuine cycle — confirmed via `turbo run build --dry` before making any change, which printed `Cyclic dependency detected: chartroom-ui#build -> chartroom#build -> chartroom-ui#build`. Fixed by splitting `chartroom`'s single `build` script (tsc + copy) into `build` (tsc only) and a new `build:ui-bundle` script/turbo task explicitly declared (via turbo.json's `<package>#<task>` override syntax) to depend on both packages' own `build` tasks — re-verified acyclic via the same `--dry` flag afterward. Root `package.json`'s `build` script updated to run both (`turbo run build build:ui-bundle`) so a single `pnpm build` still produces the complete self-contained bundle in one pass. A small, mechanical, necessary consequence of the approved decision, not a design change — flagged here since it touches files outside the phase's named file list (`turbo.json`, root `package.json`, `chartroom/package.json`'s scripts).
- **A confirmed (not hypothetical) browser-runtime break, found and fixed.** The Ctrl+K link picker's `insertLink.ts` originally imported `computeExpectedHref` from the new `chartroom/link-paths` export (an additional, not-yet-mentioned-above cross-package export this session added, since removed again — see below). `link-paths.ts` uses `node:path`'s `posix` namespace internally. An actual `npx vite build` of `chartroom-ui` was run and its output inspected directly: Vite/rolldown externalizes `node:path` for a browser bundle as an **empty stub object**, not a working polyfill — calling `computeExpectedHref` at runtime (i.e. on every Ctrl+K link insertion) would throw `TypeError: Cannot read properties of undefined (reading 'dirname')` in an actual browser with no Node.js present. Fixed by reimplementing `computeExpectedHref`/`normalizeHref` as pure string operations with zero `node:path` dependency (`chartroom-ui/src/editor/relativeHref.ts`), cross-checked against the real `chartroom` implementation across 15 path-pair cases (all match exactly), and removing the now-unused `./link-paths` export from `chartroom`'s `package.json` again. The `./markdown` export (item 3 above) is unaffected — `segmentBlocks.ts` only ever imports `AstNode` as a `import type`, which is fully erased at build time and never reaches the browser bundle, so it carries no equivalent risk. Worth a general note for future phases: **any cross-package import that will execute inside `chartroom-ui`'s browser bundle needs to be verified against a real `vite build`'s output, not just `tsc`** — type-checking success does not catch a Node-builtin dependency silently breaking at runtime.
- **`fix-links.ts`'s image-href-repair mechanism (item 4 above) needed a second mechanism beyond the plan's literal content-hash description, to actually close the gap it claims to close.** The plan's own §6.3 describes "look up its resolved absolute path's content hash against `index.assets`" — but on closer implementation, this has a real bootstrapping problem: `indexer.ts::collectAssets` only discovers/hashes an image by successfully resolving it from a doc's *currently-written* href; once a doc moves and its image href becomes stale (the literal acceptance scenario), that asset is *not* rediscoverable via any doc's resolvable reference, so it's never hashed on the next rebuild, and `index.assets` has no entry for it to look up. Implemented `computeImageFixes` with the content-hash mechanism as described **plus** a second, deterministic fallback: since this project's own upload endpoint always writes assets to `assets/<hosting-doc-id>/<timestamp>.png` (a folder keyed by the doc's own `id`, unaffected by the doc's own path), a broken href whose basename matches a file actually present under the current doc's own `assets/<id>/` folder is repaired directly from that naming convention, no hash lookup needed. Both mechanisms are implemented, tested (6 new `fix-links.test.ts` cases), and exercised end-to-end by the new acceptance script. Documented in `fix-links.ts`'s own doc comment for `computeImageFixes`.
- **`acceptance/editor-round-trip.mjs` scope, narrowed deliberately.** The plan's §9.4 describes this script also headlessly re-exercising `roundTrip.ts`'s no-op round-trip claim. `chartroom-ui`'s build output (`vite build`) is a bundled SPA, not a set of Node-importable ES modules the way `chartroom`'s own `tsc`-built `dist/` is, so a plain `.mjs` script can't reuse `roundTrip.ts` the way phase-1/phase-2's acceptance scripts reuse `chartroom`'s own CLI/daemon modules — doing so would require adding `jsdom` as a new `chartroom` dependency (not on the approved list) purely to duplicate a suite that already passes (`roundTrip.test.ts`, 40/40). The script instead covers only the image-paste-then-`git mv`-self-heal half of the acceptance line end-to-end (which lives entirely in `chartroom`'s own daemon+CLI code), with the no-op-round-trip half's coverage noted explicitly in the script's header and console output rather than silently dropped.
- **Real-browser QA — attempted, same outcome as phase 2.** Per the standing resolution above, no automated Playwright was added. A live `claude-in-chrome` click-through was not attempted this session (out of scope for the Developer stage per the phase-2 precedent, reserved for the Reviewer pass) — flagging again, now with a larger interactive surface (Ctrl+K, paste events, a mounted third-party rich-text editor) than phase 2 had, so the case for an eventual real-browser check is stronger. The structural proxy used instead: real `tsc`, `vite build`, and all three `chartroom` acceptance scripts (including the new `editor-round-trip.mjs`, itself a real Fastify `.inject()` exercise of the actual route code, not a mock) all pass.
- Nothing else needing `REMOVALS.md` beyond the throwaway spike test file already logged there (`packages/chartroom-ui/test/editor/_spike.test.ts`, left deliberately uncommitted). `team-tasks/` untouched.

## Package 3 (Chart Room phase 3): known round-trip gap on block reorder — tracked, not fixed

- **What:** the independent adversarial Reviewer found (and demonstrated with a real probe against the actual `segmentBlocks`/`roundTrip` modules, not a hypothetical) that **reordering two existing blocks** (drag/cut-paste, not a text edit) can silently reformat one of them — e.g. swapping a dash-bulleted list with an ordered list produces a spurious blank line the original bullet list never had. Root cause: `lcsMatch` (`roundTrip.ts:199-223`) requires strictly-increasing index pairs in both sequences, so a pure swap can match at most one of the two blocks by canonical-key equality; the other falls through to "new content" and gets Milkdown's freshly-canonicalized text instead of its preserved original bytes.
- **Why deferred, not fixed now:** it's a formatting regression, not data loss or corruption (the block's content stays fully intact and valid) — it only fires on a reorder action, never on the primary "edit-save cycle produces zero diff on untouched lines" no-op case, which the Reviewer confirmed held up under 23 additional novel adversarial constructs beyond the Developer's own 43-assertion suite. The reviewer explicitly recommended PASS with this as a tracked follow-up rather than a blocker, given severity.
- **Default taken:** merged to `ship-wave1` as-is. Reorder is a real but narrower editing action than the primary edit-and-save flow: someone would need to (a) drag/cut-paste-reorder two blocks, where (b) at least one has a canonical form that diverges from its original bytes (dash/plus bullets, tilde fences, setext headings, non-default ordered-list delimiters, tight lists) — plausible in a real corpus, but not the everyday case.
- **Review tomorrow:** either extend `lcsMatch` to allow a genuine one-for-one swap to match both sides (a real but bounded fix — the existing LCS-style algorithm would need a second pass for "matched sets that differ only by order," not a redesign), or accept the current behavior and document it in the editor's README as a known limitation. Should be picked up before phase 3 is considered fully hardened, not left open indefinitely.
- **Fixed:** on `ship-wave1-cr-hardening-reorder`. Implemented the "bounded second pass" option named above rather than a redesign: a new `matchReorderedBlocks` function (`packages/chartroom-ui/src/editor/roundTrip.ts`) runs after `lcsMatch` and pairs up whatever it left unmatched on both sides — for every still-unmatched *current* block, if some still-unmatched *original* block shares the exact same canonical key (comparing the same canonicalized-prose / raw-opaque keys `lcsMatch` itself compares), the two are paired and the moved block's own original bytes are spliced back in at its new position, instead of falling through to freshly re-serialized text. `reconstructFile` layers these reorder pairs on top of (never overriding) the LCS pass's own matches; the gap-preservation region algorithm deliberately still keys off the raw LCS anchors only (an out-of-order reorder pair would violate its strictly-increasing-anchor invariant), so a gap adjacent to a reordered block falls back to the standard blank-line `DEFAULT_GAP` rather than the original's preserved spacing — correct, since reordering is itself a real structural change the user made, not a phantom one. Duplicate-content ties (two blocks that canonicalize to the same key) are broken by strict positional order (first unmatched original with a key pairs with first unmatched current block with that key, etc.) — provably safe, since a pairing only ever happens between blocks whose canonical keys are already exactly equal, so two originals eligible for the same slot are by construction canonically interchangeable; this can never splice one block's bytes in as a stand-in for a *different*, canonically-distinct original block. New test coverage in `packages/chartroom-ui/test/editor/roundTrip.test.ts`: a pure two-block swap (a '+'-marker bullet list against an ordered list — the divergent-canonical-form trigger named in this entry) proving both blocks keep their own original bytes at their new position; a reorder-plus-insert composition test; a duplicate-content no-op case; and a duplicate-content-plus-reorder case documenting the positional tie-break. Plus five new pure-function unit tests directly against `matchReorderedBlocks` (swap, new-block-not-invented, deleted-block-not-invented, duplicate tie-break, surplus-duplicate-count handling). Full suite: `packages/chartroom-ui` 144/144 tests passing (was 135 before this fix — `roundTrip.test.ts` alone went from 43 to 52), `tsc --noEmit` and `eslint .` clean; `packages/chartroom` 177/177 tests passing (unchanged, confirming nothing chartroom-side assumed the old matching behavior) plus all five acceptance scripts (`git-mv-resolution`, `two-repo-browse`, `editor-round-trip`, `ask-me-round-trip`, `agent-surface-e2e`) passing, `tsc --noEmit`/`eslint .` clean there too. `editor-round-trip.mjs`'s own stale `43/43` console-log reference (accurate before this fix) updated to `52/52` to match the new count.
- **Independently re-verified by an adversarial Reviewer**, who reproduced the original bug against the pre-fix module directly (confirming it was worse than initially described — the moved list's bullet marker changed AND it gained spurious blank lines between every item) and confirmed the fix resolves it, then tried 5 additional novel scenarios beyond the Developer's own tests (three-block full reversal, reorder+delete, duplicate-content-with-one-edited in both directions, an opaque `:::llm` directive reordered against prose) — all correct. Verdict: **PASS**, content-preservation guarantee holds.
- **New, smaller, pre-existing whitespace-only defect found during this same review (not introduced by the fix):** the "denser second alignment" pass in `reconstructFile` (`roundTrip.ts:337-353`) can leak an *original* gap's spacing onto a *new* block adjacency when a reorder is combined with non-default blank-line spacing near the moved block (repro: heading → block A → 3 blank lines → block B, A and B swapped, with unrelated blocks in between — the new heading→B gap incorrectly inherits the original heading→A gap's 3 blank lines instead of falling back to the documented `DEFAULT_GAP`). Confirmed identical behavior on the pre-fix module — this predates today's fix and was simply masked by the more visible content-reformatting bug. **Affects whitespace between blocks only, never block content** — semantically inert under CommonMark rendering. **Review tomorrow:** make the denser-alignment overwrite reorder-aware (skip positions already covered by a `matchReorderedBlocks` pairing) — a small, bounded fix, same shape as today's, not urgent.

## Package 4 (Chart Room phase 4, interactive blocks + inbox) plan review — decisions taken by First Officer

Plan: `suite-design/overnight/plans/04-cr-phase4-plan.md`. Team Lead flagged 10 items in its §11; reviewed and resolved as follows so implementation isn't blocked.

- **Cross-package runtime-code export (`chartroom/interactive-blocks` imported for real, not type-only, into `chartroom-ui`'s browser bundle).** Approved — the dependency chain (`unified`/`remark-parse`/`remark-gfm`/`remark-directive`) is the same set `chartroom-ui` already bundles today with zero Node builtins, unlike the `link-paths.ts` case that broke phase 3's build. The plan's own mandated real-`vite-build` checkpoint (not just `tsc`) as the Developer stage's first step is required, not optional, given phase 3's own postmortem on exactly this failure mode.
- **Type-name mismatch resolution (`type="choice"` → alias for `single-select`, all seven real ask-human schema types as canonical vocabulary, `attachments` recognized as a capability not a type).** Approved as designed — this is careful, evidence-based reconciliation of a real spec/reality gap, not a guess.
- **`:::actions` = one directive per action item.** Approved — symmetric with the already-built `:::ask-me` shape, avoids inventing a new inline-id markdown convention `remark-directive`'s attribute model doesn't support natively.
- **Author-name capture: client-supplied `author` (localStorage-cached prompt) with server-side `os.userInfo().username` fallback.** Approved as a reasonable, low-cost UX default.
- **Attachments on ask-me answers — deferred, not built.** Approved — matches the literal acceptance line, which doesn't require them; the extension path (reusing phase 3's asset-upload endpoint) is cheap to add later if wanted.
- **No answer-correction path once `answered="true"` is set.** Accepted as a v1 limitation, not fixed this phase — logged as a tracked follow-up below, same treatment as phase 3's block-reorder gap.
- **Residual write-path race: checkbox/ask-me PATCH vs. a concurrent Milkdown full-file PUT is not fully guarded.** Accepted as a named, not-fully-solved risk for v1 — consistent with phase 3's own accepted "two Milkdown tabs, last-write-wins" risk class for a single-local-user tool. Logged as a tracked follow-up below.
- **No automated real-browser smoke test, third phase running.** First Officer will attempt the `claude-in-chrome` manual QA pass again during this phase's review (quota/session state has changed since phases 2-3's attempts); if unavailable again, the same structural-proxy substitution applies and the compounding gap will be called out clearly in the morning report rather than deferred silently a fourth time.
  - **Attempted again, still unavailable:** `tabs_context_mcp` returned "Browser extension is not connected" a second time this session (also tried once during phase 2). Now a **4th consecutive phase** without real-browser QA — flagging prominently for the morning report rather than continuing to defer silently. Notably, this phase's real bug (see below) was caught anyway, by an adversarial Reviewer reasoning carefully about jsdom-vs-real-browser `disabled`-input semantics rather than by a real browser — a useful data point that careful automated review can catch some (not all) of what real-browser testing would, but is not a full substitute.
- Nothing needing `REMOVALS.md` beyond the plan's own two small additive changes (`AstNode.checked` field, new `exports` subpath) — neither changes existing behavior. `team-tasks/` untouched.

## Package 4 (Chart Room phase 4): tracked follow-ups, not fixed this phase

- **No answer-correction path for a wrong `:::ask-me` answer.** Once `answered="true"` is set, the only fix is hand-editing the raw file outside Chart Room (Milkdown's opaque-node protection makes the block non-editable in the WYSIWYG editor by design). **Review tomorrow:** either allow re-answering by relaxing the `409` (accepting that the doc may accumulate multiple answer lines unless further designed), or build a dedicated "edit answer" affordance. Not urgent — the spec's acceptance line doesn't require correction, only that an answer lands and is read back.
- **Checkbox/ask-me PATCH writes are not guarded against a concurrent Milkdown full-file `PUT` save.** The optimistic-concurrency check protects against other PATCH writers and external (chokidar-observed) changes, but a human editing a doc's prose in one tab while a checkbox is toggled elsewhere could still have one write silently clobber the other. **Review tomorrow:** a shared in-memory file-version counter all three write paths (`PUT`, checkbox `PATCH`, ask-me `PATCH`) check against would close this, if it's ever worth the complexity for what's still a single-local-user tool.

## Package 5 (Chart Room phase 5, agent surface) plan review — decisions taken by First Officer, one flagged for the Captain

Plan: `suite-design/overnight/plans/05-cr-phase5-plan.md`. Team Lead flagged 12 items in its §12 plus a §13 whole-mission honesty check; reviewed and resolved as follows.

- **Staleness-rule-growth (`ttl_days`/`sources:` freshness gates, orphan detection) — genuine spec gap, not phase 5's (or any phase's) job.** The Team Lead traced every literal Build Order acceptance line and found this feature is mentioned in spec §6's prose exactly once, tagged "phase 2," but never actually appears in any phase's real acceptance criteria — not phase 2's, not phase 5's. **This is a mission-level, Captain-only decision, not a First Officer scope call** (unlike phase 2/4's own ambiguous-but-assignable sentences): accept it as a deliberately-dropped v1 feature (First Officer's working assumption for tonight, since neither phase that was ever plausibly "it" actually claims it), or explicitly assign it to a v1.1 pass if the Captain still wants it. **Flagging prominently in the morning report — this is not resolved, only worked around for tonight.**
- **MCP transport design (one `McpServer`, `StdioServerTransport` + `StreamableHTTPServerTransport` in stateless mode).** Approved — verified against the actual installed SDK `.d.ts` files, not guessed; stateless mode matches the "don't build machinery nothing asks for yet" discipline every prior phase established.
- **Basic Memory tool-shape mirroring — "general shape only," not literal schema mirroring.** Approved. The Team Lead hit a real 404 trying to fetch Basic Memory's actual tool reference and correctly refused to guess a schema it couldn't confirm.
- **`PostToolUse` hook: the "stronger" version** (the hook actually calls `chartroom resolve` and includes the concrete corrected path in `additionalContext`, not just a static pointer). Approved — a reasonable, small escalation past the spec's literal wording that makes the feature meaningfully more useful.
- **Failure-detection heuristic for the hook is empirically unverified — mandatory spike first.** Approved as designed: the Developer stage's first task must be triggering a real failing `Read` in a live session and capturing the actual `tool_response` shape before finalizing the detection regex, same precedent as phase 3's Milkdown spike.
- **`search` scoped to title/heading discovery, not full-text body search.** Approved — Grep already covers body search per the spec's own design north star; cheap to extend later if wanted.
- **`list_unanswered_questions` scoped to `:::ask-me` only, not `:::actions` items.** Approved — matches the tool's own name and the spec's "question" vocabulary exactly.
- **Dogfooding Chart Room onto `shareWork` itself — explicitly NOT done, deferred to the Captain.** This would be a real, one-way, repo-wide mutation (installing hooks into `.claude/settings.json`, assigning `id:` frontmatter across every doc in the repo) — squarely the kind of hard-to-reverse, shared-state-affecting action the mission's own safety posture reserves for explicit user confirmation, not something a First Officer session does as a side effect of building a feature. **Not doing this tonight; flagging as a Captain-requestable follow-up command** (`chartroom init && chartroom install-skill && chartroom install-agent-hook` against this repo, once the Captain wants it).
- **Behavioral half of the acceptance line ("a fresh Claude Code session... resolves... without human path-fixing") cannot be automated.** Accepted as an honest, structural limitation, not a shortfall — same standard prior phases applied to real-browser QA. No live Claude Code session test will be performed tonight (dogfooding is deferred, see above); this is being named plainly in the morning report rather than papered over with a script that only proves the mechanical substrate.
- **`llms.txt` only, no companion `llms-full.txt`.** Approved — matches the spec's own "bonus... for free" framing.
- Nothing needing `REMOVALS.md`; `team-tasks/` read-only for research, never modified.

## Package 5 (Chart Room phase 5): Developer-stage correction and a small plan/reality slip

- **Load-bearing correction, verified independently by two different sessions:** the plan assumed a `PostToolUse` hook fires on a failed `Read` and detects failure by string-matching `tool_response`. The Developer found, by fetching Claude Code's own hooks docs live, that `PostToolUse` fires only on tool-call *success* — a separate `PostToolUseFailure` event exists for failures, with no documented field name for the error payload. Corrected to register on `PostToolUseFailure`, gating detection on whether `chartroom resolve` finds a genuinely different path rather than guessing an error-string shape. The independent adversarial Reviewer re-fetched the same docs page itself and confirmed this is correct — the single highest-risk item in the whole phase held up under two rounds of independent verification.
- **Small factual slip, not a real issue:** the plan's §3.1 stated `resolver.ts` is reused "unmodified"; the Developer actually exported two previously-private helpers (`tokenize`/`diceCoefficient`) — a pure additive change with zero behavior change, confirmed by the Reviewer via diff and the full `resolver.test.ts` suite still passing. Noting for the record since "unmodified" turned out to mean "no behavior change" rather than "zero file diff."

## Whole-mission honesty check (from phase 5's Team Lead, surfaced not suppressed) — for the morning report

1. Spec §9's DoD line ("ask-me/checklist/llm blocks work end-to-end with a real Claude Code session") has never actually been verified with a real Claude Code session across any of the five phases — every acceptance proof this whole mission has been an automated script against APIs/CLIs, never a live agent session. Phase 5's own acceptance line is the first to name this explicitly, making the gap visible rather than something that could be waved past in more code-centric phases 1-4.
2. Chart Room has never been dogfooded on `shareWork`, its own home repo — no `.docs/index.json`, no installed hook, no doc anywhere in this repo carries a Chart-Room `id:`, despite housing every spec doc this mission implemented against all night. Doesn't block calling v1 feature-complete, but the lowest-effort way to actually *prove* it's done — turning it on for real, here — hasn't happened.

## Package 0 scaffold: no root README update

- **What:** package 0's plan flagged that the repo root has several loose docs but no root `README.md` describing the new `packages/`/`plugins/` layout; asked whether to add one.
- **Why deferred:** touching/creating root-level docs is outside this package's remit and risks scope creep on a scaffold-only package; not requested by any spec acceptance line.
- **Default taken:** skip it. `packages/README.md` and `plugins/README.md` stubs (already in the plan) are enough documentation for now.
- **Review tomorrow:** add a root `README.md` if/when the Captain wants one; trivial, reversible, no dependency on it from later packages.


## 2026-07-05 marathon: quarantined WIP adjudication (FO default in motion)
Unattributed uncommitted work over packages/chartroom + chartroom-ui found at marathon start (created 11:14-12:38, after the night session stood down; resembles queue items 2d/2e and 3: associate/open commands, claude-session/search/activity routes, RepoTree/SearchModal UI, Sidebar/RepoSwitcher deleted). Quarantined on `wip-quarantine-2026-07-05` (f34c297, pushed). FO default unless Captain overrides: v1.1 and Deck Team Leads evaluate salvage per-file during planning; anything not salvaged stays on the quarantine branch untouched. If this was YOUR work-in-progress, Captain, say so in CAPTAIN-INBOX.

## Package 3 (Captain's Deck) planning — parked questions and taken defaults (2026-07-05)

1. **Parked WIP UX features — schedule or drop?** `routes/search.ts`+SearchModal, `routes/fs.ts`
   folder picker+RegisterRepoModal, `activity.ts`/`auto-repair.ts`/`rebuild-pipeline.ts`+LatestPanel
   are in NO queue item's scope (per plan-02 FO note), so the Deck plan parks them on the
   quarantine branch. They reviewed mostly sound-with-tests; proposal: a "Chart Room v1.2 UX"
   package. Note: `routes/fs.ts` exposes full-filesystem enumeration with no auth — must be
   hardened before it ever ships.
2. **Hull package published npm name.** Local bin is `ship` (acceptance line `ship serve`), but
   the published npm name is a naming/publishing call = Captain-only. Researcher will gather
   `ship` npm availability + scoped fallbacks; no publishing tonight either way.
3. **Deck visual defaults taken tonight (low-risk, review tomorrow):** adopted the WIP's brass
   dark-only design system as the Deck look (baseline light/dark `--crm-*` system superseded);
   removed the WIP's Google Fonts external dependency in favor of local font stacks (a local
   daemon UI should not phone home). Both trivially reversible.
4. **`chartroom-ui` package rename** (it becomes the Deck app in this package) — deferred to the
   monorepo migration; keeping the name tonight to avoid Captain-invisible renames.

## Package 4 (Bridge phase 1) planning — parked questions and taken defaults (2026-07-05)

Plan: `suite-design/overnight/plans/04-bridge-phase1-plan.md` (§9). Defaults taken so
implementation isn't blocked; all trivially reversible.

1. **Changelog summarizer engine.** Ship_Spec §8 says "Haiku via Agent SDK"; the plan's default
   is `claude -p --model haiku` instead — verified working in report 02 R4 (reuses your OAuth
   login, no API key to manage, no new dependency, cents/day, JSON output). The summarizer is a
   one-file injected interface, so switching to the Agent SDK later is isolated. Override by
   naming the SDK in CAPTAIN-INBOX.
2. **Fragment noise policy.** Default: a session writes an in-repo changelog fragment only when
   it actually changed the repo (commits or dirty files); sessions with no delta still get a
   SQLite entry (truth store) but no file. Alternative: every session fragments — one-line change.
3. **Dogfood fragment commit policy for shareWork.** Hooks get enabled on this repo (authorized).
   Default: the resulting root `changelog/entries/*.md` fragments are committed — they are the
   product's shareable form and Chart Room renders them. Alternative: gitignore them until the
   monorepo migration if you'd rather this repo's history stay quiet.
4. **Possible spec-vs-CLI event gap (fact-finding pending).** Ship_Spec §2 lists
   `PermissionRequest`/`TaskCreated`/`TaskCompleted` http-hook events; the researcher pass will
   verify which actually exist in the installed Claude Code. If any are missing, Bridge phases
   2–3 (task mirroring, permission queue) need a spec amendment or an alternative mechanism —
   phase 1's acceptance (SessionStart/Stop/SessionEnd capture) is unaffected either way. Will be
   updated here with the researcher's verdict.
