---
id: report-01-housekeeping-dogfood-reviewer
---

# Reviewer report — package 01-housekeeping-dogfood

**Branch:** `ship-wave1-dogfood` (head `bf50295`) vs `ship-wave1`. **Date:** 2026-07-05.
**Verdict: FAIL** — the branch head fails its own committed acceptance script and `chartroom check`
(plan §1(d) "check exits 0, clean"). Root cause is FO rider `bf50295`, but it exposes a package-scope
gap. Everything else in the package is solid; remediation is small (see §5).

## 1. What I executed (all commands from repo root unless noted)

| Command | Result |
|---|---|
| `pnpm turbo run build lint test` | 6/6 green (cached) |
| `pnpm turbo run build lint test --force` | 6/6 green, 0 cached, 24.9s |
| `pnpm --filter chartroom test` | 25 files / 181 tests green, incl. `chartroomignore.test.ts` (4 tests) |
| `node packages/chartroom/dist/cli.js check` (branch head) | **exit 1** — `Docs missing an id (1): suite-design/overnight/PROGRESS.md` |
| `node packages/chartroom/acceptance/dogfood-sharework.mjs` | **exit 1** — fails step 1 assertion (`expected 'chartroom check' to exit 0, got 1`) |
| Scratch script re-running acceptance steps 2+3 in isolation | **PASS** (details §2) |
| `chartroom check` in a detached worktree at `22e2045` (package's last own commit) | **exit 0, clean** |
| Byte-purity audit script over every 4-line-prepend md in the diff | 42/42 byte-pure, 0 violations (details §3) |

Worktree `wt-22e2045` was created under the reviewer scratchpad and removed via `git worktree remove`
after the check (verified gone from `git worktree list`).

## 2. Acceptance line — demonstrated (steps 2+3), but the committed script fails (step 1)

Acceptance line: "a `git mv` of a suite-design doc self-heals; the changelog directory renders in the viewer."

- **git mv self-heal: PASS.** I performed `git mv suite-design/Product-Suite_Research-Synthesis.md
  → ….tmp-moved.md`; `chartroom resolve product-suite-research-synthesis-july-2026 --json` returned
  matchType `id` at the NEW path; raw `.docs/index.json` contained the new path; `git mv` back;
  resolve returned the original path. Both git mvs executed and restored, net-zero confirmed via
  `git status --porcelain` on both paths (empty).
- **Changelog renders: PASS.** Real daemon `buildServer` in-process (same pattern as the committed
  script): `GET /api/repos` lists sharework; `GET /api/repos/sharework/docs` lists all 8
  `suite-design/overnight/changelog/entries/*.md` with ids; `GET /api/repos/sharework/docs/package-0-monorepo-scaffold`
  → 200 with raw content containing its `id:` line.
- **BUT** the package's own committed gate, `packages/chartroom/acceptance/dogfood-sharework.mjs`,
  exits 1 on the branch head (step-1 assertion `check exits 0`), and plain `chartroom check` exits 1.

### Root cause (reproducible)

`f5d331f` (the init commit) gave `suite-design/overnight/PROGRESS.md` `id: mission-progress`.
FO rider `bf50295` bumped progress by re-running `suite-design/overnight/render-progress.mjs`, which
**rewrites PROGRESS.md wholesale from progress.json and does not preserve frontmatter** — the diff of
`bf50295` shows the 4 id lines deleted. `git show f5d331f:suite-design/overnight/PROGRESS.md` has the
id; branch head does not. Check was clean at `22e2045` (worktree-verified, exit 0).

### Why this is package-relevant, not just an FO slip

The plan (§4) deliberately swept `suite-design/overnight/**` tracking files into id scope, including
PROGRESS.md — a file whose own header says "Generated by render-progress.mjs … Do not edit by hand."
Nothing in the package excludes it or teaches the generator to preserve/emit frontmatter, so **every
future progress bump re-breaks `chartroom check`** (and, post-hook-install, causes id churn at every
commit that touches it). The very first regeneration after the package finished broke the gate — a
recurring landmine, not a one-off.

## 3. Byte-purity audit — 42/42 clean

Programmatic audit (scratchpad `audit_prepends.py`): for every `*.md` in the diff with numstat
`+4/-0` (42 files), asserted new bytes == `---\n` + `id: …\n` + `---\n` + `\n` + old bytes exactly
(git cat-file blobs, byte comparison). 0 violations. The 13 remaining md diffs are package
deliverables (CLAUDE.md, SKILL.md install, README §, changelog fragment, plan §10 deviations,
team-lead report, CAPTAIN-TODO) or the two FO riders (`56c538d`, `bf50295` — excluded from
scope-creep judgment per dispatch).

## 4. Everything else checked — clean

- **Exclusion zones untouched:** `git diff --name-only` grep for `team-tasks/`,
  `packages/chartroom-ui/test/editor/fixtures/`, `packages/chartroom/skill-template/`,
  `suite-design/lookout/` → zero hits. Round-trip suite green (52 tests) — fixtures intact.
- **`.chartroomignore` enabling change** (`packages/chartroom/src/repo.ts:49-63`): additive to
  `.gitignore`, same `ignore` instance, funnels through `discoverDocFiles` which init/indexer/
  fix-links/daemon-watcher all use (grep-verified). Tests cover discovery exclusion, init byte-identity
  on excluded files, check not counting excluded docs, and no-file-present fallback. Matches plan §2–3.
  `runInit` export in `init.ts` is the only other src change.
- **Root `.chartroomignore`** matches plan §4 exactly.
- **Skill install verbatim:** `.claude/skills/chart-room/SKILL.md` is byte-identical to
  `packages/chartroom/skill-template/chart-room/SKILL.md` (diff empty).
- **CLAUDE.md** carries the Chart Room template section + the ignore-scope warning; has `id: sharework`.
- **Parked seam honest:** `install-agent-hook` permission-denied, recorded in plan §10 deviations and
  CAPTAIN-TODO.md with the exact one-line human command (`node packages/chartroom/dist/cli.js
  install-agent-hook`); post-merge pre-commit hook install also parked with exact steps. No fake, no
  mocked-and-claimed integration. Per dispatch, not a FAIL cause.
- **Pre-existing working-tree dirt** (team-tasks/*, watchdog.log, untracked Captain files) untouched
  by the package; none staged.

## 5. Remediation options for the FO (any one unblocks a re-review PASS)

1. Make `render-progress.mjs` frontmatter-preserving (read existing id, re-emit it — ~5 lines), then
   re-run it (restores `id: mission-progress`), rebuild index, re-run the acceptance script. Best fix:
   kills the recurring landmine.
2. Or add `suite-design/overnight/PROGRESS.md` to `.chartroomignore` (it is derived state, like
   `.docs/index.json`) and re-run `init --no-hook` + acceptance.
3. Minimum (not recommended alone): re-inject the id on PROGRESS.md — check goes green until the next
   progress bump strips it again.

## 6. File pointers

- Regression: `suite-design/overnight/PROGRESS.md` (no frontmatter at head) vs `git show f5d331f:…`;
  stripper: `suite-design/overnight/render-progress.mjs` (writes file wholesale, no frontmatter).
- Failing gates at head: `node packages/chartroom/dist/cli.js check` (exit 1),
  `node packages/chartroom/acceptance/dogfood-sharework.mjs` (exit 1, step-1 assertion at line 70).
- Enabling change: `packages/chartroom/src/repo.ts:49-77`; tests
  `packages/chartroom/test/chartroomignore.test.ts`; root `.chartroomignore`.
- Seam: `suite-design/overnight/CAPTAIN-TODO.md` (two dogfood entries); plan deviations:
  `suite-design/overnight/plans/01-housekeeping-dogfood-plan.md` §10.

---

# RE-REVIEW (scoped) — remediation of the FAIL above

**Date:** 2026-07-05. **Branch head:** `5d4c316` (`ship-wave1-dogfood`). **Verdict: PASS.**
Scope per dispatch: verify remediation commits `d246a8c` + `5d4c316` only; everything else
stands as verified in the original review above.

## 1. Remediation diff read (skeptical)

- `git diff --name-only 2759d36..HEAD` → exactly 3 files: `suite-design/overnight/PROGRESS.md`,
  `suite-design/overnight/render-progress.mjs`, `suite-design/overnight/reports/01-housekeeping-developer.md`.
  **No scope creep.**
- `render-progress.mjs`: new `readExistingFrontmatter()` captures a leading
  `^---\r?\n[\s\S]*?\r?\n---\r?\n\r?\n?` block from the existing output file and re-emits it
  verbatim before the rendered body; separator logic guarantees a blank line after the block.
  Regex is `^`-anchored (no false match on the body's later `---` rule), CRLF-tolerant,
  non-greedy. If the file is absent or has no frontmatter it emits none — acceptable; a
  missing id would still be caught by `chartroom check`.
- `Last updated` derives from `progress.json` package dates (reduce at
  `render-progress.mjs:81`), not wall clock — regeneration is deterministic, so
  idempotency is structural, not luck.
- `PROGRESS.md`: exact 4-line id block restored (`id: mission-progress`), matching `f5d331f`.
  The one body-line change (#1 note) is the renderer syncing to already-committed
  `progress.json` — generator output, not hand-editing.
- Developer report `5d4c316` is docs-only, carries its own chart-room id.

## 2. Executed at head (all from repo root)

| Command | Result |
|---|---|
| `pnpm turbo run build lint test --force` | 6/6 tasks green, 0 cached, 10.9s |
| `pnpm --filter chartroom test` | 25 files / 181 tests green |
| chartroom-ui tests (via turbo) | 15 files / 139 tests green |
| `node packages/chartroom/dist/cli.js check` | **exit 0** — clean |
| `node packages/chartroom/acceptance/dogfood-sharework.mjs` | **exit 0** — steps 1–3 OK, ALL ASSERTIONS PASSED |
| `node suite-design/overnight/render-progress.mjs` (run 1) | `git diff --exit-code PROGRESS.md` → 0 (zero diff), frontmatter intact |
| `node suite-design/overnight/render-progress.mjs` (run 2) | zero diff again; `head -5` shows id block; `chartroom check` after regen → exit 0 |

The original FAIL's recurring landmine (every progress bump strips the id) is dead: regenerating
from committed state is a byte-level no-op and preserves the frontmatter.

## 3. Verdict

**PASS.** Acceptance line demonstrably holds via the committed script at head; both gates that
failed the original review now exit 0; regeneration is idempotent and frontmatter-safe; test
suites green; remediation contains exactly the promised fix plus its report, nothing else.
This PASS covers the whole package (original review §§2–4 verified everything outside the
remediation).
