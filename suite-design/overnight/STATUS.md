# Live Status Board

Legend: pending / plan-review / in-progress / PASS / FAIL / parked

| Package | Status | Branch | Note |
|---|---|---|---|
| 0. Monorepo scaffold | **PASS — merged, NOT pushed** | ship-wave1-scaffold → ship-wave1 (f57ee02..8a862ea, fast-forward) | pnpm+turborepo scaffold, packages/+plugins/ stubs, team-tasks/ verified untouched; `git push origin ship-wave1` blocked by this session's permission mode (no interactive approver) — Captain needs to push manually or grant Bash permission |
| Lookout (infra, not a wave-1 package) | **PASS — merged, NOT pushed** | ship-wave1-lookout → ship-wave1 (29ee8f8, 83b5c1e) | Sensor-only usage poller, proven live; running again this session (materialized from `ship-wave1` into this branch's working tree, uncommitted here — see log) |
| 1. Chart Room phase 1 (indexer/CLI) | **PASS — merged, NOT pushed** | ship-wave1-cr-phase-1 → ship-wave1 | Full foundation: indexer, 5-step resolver, tombstone/move/resurrection lifecycle, AST-safe link repair, CLI (init/index/resolve/fix-links/check), staged-blob pre-commit hook, acceptance script. 8 files / 53 tests, tsc/eslint/acceptance all green, run live pre-merge. Independent adversarial Reviewer PASS. |
| 2. Chart Room phase 2 (viewer) | **PASS — merged, NOT pushed** | ship-wave1-cr-phase-2 → ship-wave1 | Fastify daemon + registry + chokidar watchers, new `chartroom-ui` (React/Vite) with pretty rendering/TOC/collapsing/backlinks/images/`:::llm`+`:::human`/tombstone display. 11+3 files / 68+15 tests, tsc/eslint/both acceptance scripts green, real daemon boot + live curl verified twice (Developer + independent Reviewer). Browser click-through QA not possible (Chrome extension not connected this session) — real-daemon curl proxy substituted, gap logged in DECISIONS-NEEDED.md for follow-up. |
| 3. Chart Room phase 3 (editor) | **PASS — merged, NOT pushed** | ship-wave1-cr-phase-3 → ship-wave1 | Milkdown editor via block-level diff-and-splice round-trip engine (opaque-node passthrough for directives/HTML/frontmatter), image paste, Ctrl+K link picker. 43/43 round-trip assertions + 23 independent adversarial constructs all byte-identical. One known gap tracked (not fixed): block reorder can reformat an untouched block in some cases — logged in DECISIONS-NEEDED.md, not a data-loss bug. |
| 4. Chart Room phase 4 (interactive blocks) | plan-review → in-progress | ship-wave1-cr-phase-4 | Plan approved (single-block server-side splice endpoints, not editor-path reuse; ask-me schema mismatch resolved; actions=one-directive-per-item) — dispatching Developer. |
| 5. Chart Room phase 5 (agent surface) | pending | — | blocked on #4 |

## Log
- 2026-07-05 ~00:30 — First Officer session started, fresh (attempt #1, no resume). Created PLAN.md, STATUS.md, DECISIONS-NEEDED.md, REMOVALS.md. Creating `ship-wave1` branch next.
- 2026-07-05 ~00:35 — `ship-wave1` created from main HEAD, specs + tracking board committed (f57ee02). Feature branch naming defaulted to dashes (`ship-wave1-scaffold` not `ship-wave1/scaffold`) — see DECISIONS-NEEDED.md. On `ship-wave1-scaffold` now, dispatching Team Lead for package 0.
- 2026-07-05 (resumed instance) — Reconciled: `ship-wave1-scaffold` has 0 commits beyond f57ee02 and `suite-design/overnight/plans/` is empty, so the previous instance's "Team Lead dispatched, writing plan" never produced a durable plan (session likely ended before the Lead returned). Treating package 0 as not-yet-started; re-dispatching Team Lead from scratch. No repo work was lost since nothing had been committed.
- 2026-07-05 (resumed instance) — Noted pre-existing **uncommitted** working-tree changes unrelated to this mission: root `.gitignore`, `team-tasks/.gitignore`, `team-tasks/src/app/(app)/settings/page.tsx`, `team-tasks/src/app/onboarding/page.tsx`, `team-tasks/src/lib/supabase/middleware.ts` (modified), and `team-tasks/src/app/(app)/settings/invite-section.tsx` (untracked/new). These predate this session and are not part of any wave-1 package. Per mission rules (never touch `team-tasks/`, never guess), left entirely untouched — not staged, not committed, not reverted. Will avoid `git add -A`/`git add -u` for this reason and stage only the exact paths each package touches. Flagging for the Captain in the morning report as "found dirty, left alone."
- 2026-07-05 (resumed instance) — Package 0 Team Lead plan found on disk (`plans/00-scaffold-plan.md`), reviewed: additive-only, isolates `team-tasks/` via narrow pnpm globs, researcher-verified Turborepo v2 `tasks` schema, honestly scopes DoD for a package with no real buildable member yet. **Approved** with two directions: skip root README (logged in DECISIONS-NEEDED.md), and the `git status`-clean-under-team-tasks/ gate must be actually run and its output captured, not just asserted. Dispatching implementation now.
- 2026-07-05 — Package 0 implemented and adversarially self-reviewed by the Team Lead: **PASS**. 4 commits on `ship-wave1-scaffold` (`b7c90f9` scaffold config, `e58d7c2` README stubs, `4d12acc` pnpm lockfile, `8a862ea` changelog entry). Reviewer independently confirmed: exactly 11 files touched total, zero `team-tasks/` paths in the diff, `.gitignore` change is a pure append (pre-existing `.vercel`/`.env*` lines untouched), `turbo run build`/`lint` no-op clean on the README-only workspace, `pnpm install` idempotent. One pre-authorized deviation: `packageManager` pin corrected from placeholder `pnpm@10.x` to the real resolved `pnpm@10.34.4+sha512...` via corepack. Fast-forward merged `ship-wave1-scaffold` → `ship-wave1` (f57ee02..8a862ea). Noted (not acted on): a live spec annotation appeared mid-session in `suite-design/Ship_Spec.md` §6 signed "Ondřej — later improvement" about wanting a better live mission view in the console — outside scope of any current package (Bridge/console is package 6+, only if capacity remains); left untouched, will surface in the morning report. Moving to package 1 (Chart Room phase 1).
- 2026-07-05 — Tried `git push origin ship-wave1` twice: blocked both times by this session's permission mode ("Claude requested permissions to use Bash, but you haven't granted it yet") — not a network/auth error, an interactive-approval gate with no approver present overnight. Not retrying further tonight (would just repeat the same block); will retry once after each subsequent accepted package in case the mode changes, and will flag prominently in MORNING-REPORT.md either way. All work remains safely committed locally on `ship-wave1`/feature branches.
- 2026-07-05 (resumed instance, new attempt) — Reconciled on `ship-wave1-cr-phase-1`: `git diff --stat ship-wave1..ship-wave1-cr-phase-1` is empty (zero commits ahead) and `suite-design/overnight/plans/` still contains only `00-scaffold-plan.md` — the previous "Team Lead dispatched to write plan" for package 1 died before producing anything durable, identical to package 0's first failed attempt. No work lost (there was none). Re-dispatching Team Lead for package 1 from scratch. Also: confirmed no other Claude/node/pwsh process is currently running on this machine (checked `tasklist`) — no concurrent-mission risk right now, per the mission-lock warning Ondřej left in `Trio_Specs.md` §B (two First Officer sessions collided twice on night one; no lockfile exists yet, forensic detection only). Retried `git push origin ship-wave1`: blocked again, identical permission-mode message. Not retrying again until package 1 is accepted. Checked `usage.json`: 15% of 5-hour window, 56% of 7-day, no PAUSE file — plenty of runway, proceeding.
- 2026-07-05 — Package 1 plan reviewed (`plans/01-cr-phase1-plan.md`, ~580 lines, thorough: full index schema, resolution algorithm, tombstone/move-vs-deletion lifecycle, blob-level pre-commit hook design, test plan + acceptance script mapped to every spec acceptance line). Checked for scope creep against §8.1 — none found, phases 2-5 items correctly excluded. Verified no existing `.git/hooks/pre-commit` and no husky config under `team-tasks/` (so the hook-install collision risk the Lead flagged is moot tonight). Resolved all 4 of the Lead's flagged decisions (see DECISIONS-NEEDED.md for full reasoning): approved package name, approved the dependency list as covered by the kickoff prompt's own stack section + DoD requirements, approved the no-hook-chaining-framework approach, and **overrode** `fix-links`'s default to dry-run (must pass `--write` to mutate) rather than the plan's proposed mutate-by-default. **Approved for implementation.** Instructed the Team Lead to proceed: install approved deps, dispatch Developer(s), commit incrementally, then Reviewer pass before reporting back.
- Noted, not acted on (out of scope for any current package): live-edited working-tree diffs in `Ship_Spec.md` §6 and `Trio_Specs.md` §B (Ondřej's "later improvement"/"elevate to full-fledged feature" annotations about a console mission-view and a productized quota-aware-pause + mission-lock feature) and a rewritten `OVERNIGHT-KICKOFF-PROMPT.md` (adds the plan-review/PAUSE protocol this session is already following, since it reads the working-tree file) — all uncommitted product-input notes, left untouched, will surface in MORNING-REPORT.md. `team-tasks/` dirty files also still present and still untouched (unrelated user work in this same repo, not a wave-1 concern).
- 2026-07-05 (resumed instance) — Reconciled: no `PAUSE` or `DONE` file present, on `ship-wave1-cr-phase-1`, 8 commits ahead of `ship-wave1` (`204628b`..`1e8c766`). Verified against plan: only the lib layer of package 1 is actually done and committed — `src/{repo,frontmatter,id,markdown,index-schema}.ts` + matching unit tests (`686d0b7`, `4e020ef`, `1e8c766`); `tsc --noEmit` and `vitest run` both clean (27/27). `src/commands/` and `acceptance/` exist but are still empty — CLI commands, pre-commit hook, and the acceptance script from the approved plan are not yet implemented, matching the "in-progress" status honestly (not overclaimed). Checked `tasklist`: multiple `claude.exe`/`powershell.exe` processes are running on this machine, but process list alone can't confirm any of them are a second First Officer in *this* repo (no mission-lock file exists tonight, per the known gap logged in `Trio_Specs.md` §B) — proceeding since there's no PAUSE/lock evidence, but flagging this as unresolved risk rather than asserting safety. `team-tasks/` dirty files still present, still untouched. Dispatching Team Lead to continue package 1 from current state: implement CLI commands (init/index/resolve/fix-links/check), pre-commit hook, and the acceptance script per the already-approved plan, then adversarial Reviewer pass, then report back.

## Reconciliation — new First Officer session ("final"), post-overnight-kill

Continuing directly on `ship-wave1-cr-phase-1` (the only branch the mission scope allows tonight). Findings, git/disk-verified, not taken from any prior session's testimony:

- **Package 0 (scaffold):** confirmed merged into `ship-wave1` (`f57ee02..8a862ea`, fast-forward, matches STATUS's own claim). Not independently re-verified beyond the git graph (out of tonight's scope — Chart Room only) but nothing contradicts it.
- **Lookout package:** confirmed merged into `ship-wave1` (`29ee8f8` lookout.ps1 + PLAN.md, `83b5c1e` changelog) — `ship-wave1-lookout` and `ship-wave1` tips are identical. **Important divergence found:** `ship-wave1-cr-phase-1` branched from `ship-wave1` at `09c431e`, *before* the lookout commits landed on `ship-wave1` — so the lookout files do not exist in this branch's history or working tree. Confirmed by `ls suite-design/lookout` failing on this branch. Materialized `lookout.ps1`+`PLAN.md` into the working tree via `git show ship-wave1:...` (untracked here, not staged/committed — this is infra, not a chart-room concern) so the sensor could actually be started per mission rules. Lookout confirmed running and healthy: `state/usage.json` fresh (44% five-hour / 59% seven-day at 03:40, status `ok`), well under the 80% ALERT line.
- **Chart Room phase 1 — the real state, verified by running the tools, not by reading claims:**
  - Lib layer complete and committed through `f70fa2d`: `repo.ts`, `frontmatter.ts`, `id.ts`, `index-schema.ts`, `markdown.ts`, `link-paths.ts`, `resolver.ts` (5-step resolution, `1e086be`), `indexer.ts` (full-repo scan + tombstone/move/resurrection diff, `f70fa2d`) — this is *more* than STATUS's last log entry recorded (that entry stopped at `1e8c766`; three more commits landed after it and were never logged, confirming the mid-package kill happened after this point, not before).
  - **New finding: `src/fix-links.ts` + `test/fix-links.test.ts` exist and are fully working but were never committed** — sitting as untracked files in the working tree since before this session started. This is the actual "killed mid-package" moment: the link-repair logic (plan §6.2) was finished, including the code-fence/dangling-id/multi-fix-no-offset-drift edge cases, but the commit never happened.
  - Verified health directly: `npx tsc -p tsconfig.json --noEmit` clean, `npx vitest run` → **7 files / 48 tests, all green** (including the 6 uncommitted `fix-links` tests).
  - **Still not started at all** (no directories exist yet, contrary to the prior session's note that they were "empty" — they're simply absent): `src/cli.ts`, `src/hook.ts`, `src/install-hook.ts`, `src/check.ts`, `src/commands/{init,index,resolve,fix-links,check}.ts`, `test/hook.test.ts`, `acceptance/git-mv-resolution.mjs` + its README, the package `README.md`, and the phase-1 changelog entry. This is the CLI wiring + pre-commit hook + acceptance-script layer from the approved plan (`plans/01-cr-phase1-plan.md` §3/§8/§9/§11.2) — genuinely 0% done, not partially done.
- **Delivered vs. claimed vs. salvageable, summarized:** package 0 and Lookout — delivered as claimed. Chart Room phase 1 — lib layer + link-repair logic delivered and verified (slightly ahead of STATUS's last claim); CLI/hook/acceptance layer is honestly 0% and was never claimed otherwise. Salvageable WIP: the uncommitted `fix-links.ts`/test pair — verified working, committing as-is before continuing.
- Next action: commit the verified `fix-links.ts`/test pair, then dispatch a Developer to implement the remaining CLI/hook/acceptance layer per the already-approved plan (no new Team Lead planning pass needed — the plan already fully specifies these files), then an adversarial Reviewer pass before merge to `ship-wave1`.
- Committed `fix-links.ts`+test as-is (`cceaff5`). Also found and fixed a real gap while reconciling: nothing in the repo gitignored `.docs/` (the runtime index directory both the spec and `repo.ts` assume is gitignored) — added it to root `.gitignore` (`4bb3688`).
- Dispatched a Developer to implement the remaining plan-specified files (CLI wiring, pre-commit hook + install shim, check command, hook tests, acceptance script, package README). Result: 5 commits (`b1a50c5` check.ts, `938657d` hook.ts+install-hook.ts+hook.test.ts, `43ac169` cli.ts+commands/*, `bce3a2a` acceptance script+README, `f7adf51` package README). Self-reported 53/53 tests green, tsc/eslint clean, acceptance script all-assertions-passed, hook verified against a real (scratch, non-repo) `git commit`.
- Dispatched an independent adversarial Reviewer (no context from the Developer run). Its sandbox had no shell access, so its PASS was based on full line-by-line static reading of every src/test file plus circumstantial build artifacts (dist/ matching src/, cached vitest results) — verdict **PASS**, with two non-blocking reservations: (1) `spike.mjs` research scaffolding left in the package (logged to `REMOVALS.md`, harmless — excluded from build/lint/publish), (2) pre-existing unrelated dirty `team-tasks/`/`suite-design/` files should be excluded from any PR diff (moot for a local branch merge, which only moves committed history).
- Because the Reviewer couldn't execute anything itself, ran all four verification gates myself live before trusting the PASS: `npx tsc -p tsconfig.json --noEmit` → exit 0. `npx vitest run` → **8 files / 53 tests, all green** (including all 5 `hook.test.ts` cases: fully-staged, partial-staging, missing-id, not-staged, never-commits). `npx eslint .` → exit 0, 0 problems. `npm run build && node acceptance/git-mv-resolution.mjs` → **ALL ASSERTIONS PASSED**, exit 0. Static PASS confirmed by execution — no discrepancy found.
- **Chart Room phase 1 accepted: PASS.** Changelog entry written (`changelog/entries/2026-07-05--cr-phase1.md`). Proceeding to fast-forward merge `ship-wave1-cr-phase-1` → `ship-wave1`, then starting phase 2 (viewer) per the mission's phase order.
- Merge was **not** a fast-forward (`ship-wave1` had diverged with the 2 lookout commits phase-1's branch never had). Real merge commit (`4671ef0`), one conflict in `.gitignore` (lookout's `state/` line vs. phase-1's `.docs/` line) resolved by keeping both lines. Post-merge, re-ran `tsc --noEmit` + `vitest run` on `ship-wave1` itself (not just the feature branch): 8 files / 53 tests, all green. Confirmed the only untracked pre-existing dirty files remaining are the same ones logged at session start (`team-tasks/`, loose `suite-design/*` prompt files) — nothing new introduced.
- Created `ship-wave1-cr-phase-2` off `ship-wave1`. Dispatched a Team Lead (Plan agent) to write the phase-2 (viewer) plan. Its sandbox had no file-write tool, so it returned the full plan as text; First Officer persisted it to `plans/02-cr-phase2-plan.md` verbatim. Plan: two packages (`chartroom` gains a Fastify daemon + chokidar watchers + repo registry; new `chartroom-ui` is a private React 19/Vite 8 frontend copied into `chartroom`'s publishable `dist/`), `react-markdown` + `remark-directive`(-rehype) for `:::llm`/`:::human` rendering, hand-rolled `<details>`-based section collapsing (no fitting library found), hash-based nav (no router), backlinks/tombstone display both computed by directly reusing phase-1's `indexer.ts`/`check.ts` with zero schema changes. Flagged 8 items for First Officer/Captain in its §11 — all resolved same-session (see `DECISIONS-NEEDED.md`): two-package split approved, full dependency list approved (chokidar pinned to v4 to avoid an engines bump), staleness scope read narrowly (tombstone display only, no dashboard), Mermaid deferred, Playwright skipped in favor of a manual `claude-in-chrome` browser QA pass during review, register-only (no unregister) approved. **Plan approved**, dispatching Developer.
- Developer implemented phase 2 in 5 commits (chartroom-ui scaffold, rendering pipeline/components, daemon backend, CLI wiring, acceptance script). Self-reported 68/68 + 15/15 tests green across both packages, real daemon boot verified via live `curl` (repo list, tombstone data with a `deletedAt` enrichment the plan hadn't accounted for, raw asset fetch, path-traversal rejection). Four honest deviations flagged: 4 extra direct deps in `chartroom-ui` (transitive members of already-approved packages, needed because pnpm doesn't expose transitive deps for direct import), a `deletedAt` enrichment added in the docs route rather than `check.ts` (phase-1 file left untouched), a frontmatter-strip fix for a real CommonMark setext-heading bug, and a TOC-link/hash-routing conflict fix.
- Independent adversarial Reviewer (with real Bash access this time) verified all four deviations independently — confirmed each via `pnpm why`, direct code reading, and a standalone repro of the setext-heading bug — and re-ran every verification command itself plus its own from-scratch daemon boot (traversal test with `--path-as-is` even confirmed the double-encoded case is rejected, which the Developer's own test hadn't tried). **Verdict: PASS**, one non-blocking reservation (an unrecognized directive works by React's default passthrough rather than an explicit `DirectiveFallback` route — functionally fine, cosmetically inconsistent, noted for later).
- Attempted the manual browser QA pass myself per the DECISIONS-NEEDED.md resolution: built both packages for real, scaffolded two throwaway scratch repos (one with a real tombstone), registered them with `HOME` sandboxed to a temp dir, booted `chartroom serve --port 4917` in the background. `mcp__claude-in-chrome__tabs_context_mcp` returned "Browser extension is not connected" — could not do the actual click-through. Substituted live `curl` against the running daemon as the closest available proxy: `GET /` served the real built `index.html` referencing an asset file confirmed present on disk; `GET /api/repos/repoa/docs/repoa-index` returned the correctly-enriched tombstone data. Killed the daemon process afterward (confirmed port stopped responding). Logged the unfinished browser QA as a specific follow-up in `DECISIONS-NEEDED.md`.
- **Chart Room phase 2 accepted: PASS** (with the browser-QA gap honestly logged, not hidden). Changelog entry written (`changelog/entries/2026-07-05--cr-phase2.md`). Merging `ship-wave1-cr-phase-2` → `ship-wave1`.
- Merge was clean this time (no `.gitignore`-style conflict). Re-verified `tsc --noEmit` + `vitest run` on `ship-wave1` itself post-merge for both `chartroom` (68/68) and `chartroom-ui` (15/15). Created `ship-wave1-cr-phase-3` off `ship-wave1`.
- Dispatched a Team Lead (general-purpose agent, given write tools directly this time to avoid the phase-2 copy-paste step) to plan phase 3 (the editor). It produced an unusually thorough, honest plan: confirmed via real research (npm registry + GitHub Discussions/DeepWiki, since milkdown.dev's own docs render client-side and weren't fetchable) that whole-document Milkdown serialization would fail phase 3's acceptance line on essentially every real doc (canonicalizes bullet/heading style) *and* independently corrupt `:::llm`/`:::human`/`:::ask-me` directive blocks (Milkdown's presets don't parse `remark-directive` at all). Designed a block-level diff-and-splice engine instead (mirrors phase 1's `fix-links.ts` byte-splice discipline): segment the doc into top-level blocks, classify each as editable "prose" or protected "opaque" (directives/HTML/frontmatter, strict allowlist so anything unrecognized defaults to protected), and on save splice back original bytes for unchanged blocks / fresh serialization only for genuinely edited ones. Flagged 10 items in its §11, all resolved same-session (see DECISIONS-NEEDED.md): round-trip strategy approved, dependency list approved (`@milkdown/kit` not `@milkdown/crepe`, `fuse.js`, `remark-frontmatter`), a cross-package export addition from phase 1 approved (correctness-critical reuse, breaks phase 2's duplication precedent for a good reason), a narrow `fix-links.ts` behavior extension for image-href repair approved, asset-folder semantics/explicit-save/hand-rolled-Ctrl+K all approved as recommended, no-Playwright resolution repeated from phase 2. **Plan approved**, dispatching Developer. This is the highest-risk phase so far — watching closely.
- Developer did a real Milkdown API spike before writing production code (per the plan's own honesty flag) and found one real design-invalidating conflict: `@milkdown/preset-commonmark` registers its own `html` node schema as an *inline* atom, which broke the idea of feeding whole raw text through Milkdown's parser. Resolved by following the plan's literal per-block-construction design (never feed opaque syntax through Milkdown's own markdown parser at all) rather than a simplification attempted first. Also found and fixed, unprompted: a circular workspace dependency the approved cross-package export introduced (fixed by splitting `chartroom`'s build script + a `turbo.json` task override, verified acyclic via `--dry`); a confirmed (not hypothetical) browser-runtime bug where `node:path` doesn't work in a Vite bundle (verified by inspecting the actual built JS, fixed by a zero-dependency local reimplementation cross-checked against the real function across cases); a real bootstrapping gap in the plan's hash-only image-repair design, closed with a second fallback mechanism. All 5 deviations logged in `DECISIONS-NEEDED.md`. Self-reported 85/85 + 100/100 tests (43/43 in the round-trip suite specifically), all three `chartroom` acceptance scripts green.
- Independent adversarial Reviewer ran everything itself and went further: hand-wrote 23 novel markdown constructs the Developer's suite didn't cover (single-quoted link titles, escaped table pipes, back-to-back directives, nested code-in-list-item, CRLF, unicode, etc.) and ran them through the real modules directly — all byte-identical. Found one genuine, reproducible gap the Developer's tests didn't exercise: **reordering two existing blocks can reformat one of them** if its canonical form diverges from its original style (e.g. swapping a dash-bulleted list with an ordered list adds a spurious blank line to the untouched list) — root-caused to the LCS block-matching algorithm only handling insert/delete at fixed positions, not a pure swap. Verdict: **PASS**, with this logged as a tracked follow-up rather than a blocker (formatting regression, not data loss; doesn't affect the primary edit-and-save flow).
- Logged the reorder gap in `DECISIONS-NEEDED.md` with root cause and a concrete fix direction for later. **Chart Room phase 3 accepted: PASS.** Changelog entry written (`changelog/entries/2026-07-05--cr-phase3.md`). Merging `ship-wave1-cr-phase-3` → `ship-wave1`.
- Merge clean, re-verified `ship-wave1` itself post-merge: `chartroom` 85/85, `chartroom-ui` 100/100. Lookout 5-hour usage window reset mid-review (dropped from 72% back to 2%) — plenty of runway. Created `ship-wave1-cr-phase-4` off `ship-wave1`.
- Dispatched a Team Lead to plan phase 4 (interactive blocks + inbox). It caught a real spec/reality mismatch: §4.1's own example uses `type="choice"`, but the actual `.claude/skills/ask-human/SCHEMA.md` vocabulary (which the spec says to reuse) has no such type — resolved via an explicit alias map (`choice`→`single-select` etc.) rather than guessing, with all seven real schema types implemented as the canonical vocabulary and `attachments` correctly identified as a capability, not an eighth type. Designed a write-back mechanism deliberately different from phase 3's: three narrow server-side PATCH endpoints (checkbox toggle, ask-me answer, inbox read) that re-read the file fresh from disk and splice narrowly, rather than reusing phase 3's client-side full-file reconstruction — justified because `DocView` (read-only viewer) never loads Milkdown, and reusing the editor's save path would be both wasteful and carry a worse staleness risk for the common "many small checkbox clicks scattered over a session" case. Flagged a real, larger cross-package export decision (actual runtime code, not just a type, imported into `chartroom-ui`'s bundle this time) with a mandatory real-`vite-build` verification checkpoint given phase 3's own postmortem on exactly this failure mode. Resolved all 10 of its §11 items same-session (see DECISIONS-NEEDED.md), including accepting two new tracked-not-fixed limitations (no answer-correction path once answered; a residual write-path race between checkbox/ask-me PATCH and a concurrent Milkdown PUT) rather than over-engineering solutions the acceptance line doesn't require. **Plan approved**, dispatching Developer.
