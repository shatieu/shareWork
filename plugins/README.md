---
id: plugins
---

# plugins/

Claude Code plugin-distributed pieces of the suite (Crew, chart-room-skill,
template-packs — see `suite-design/Ship_Spec.md` and `ChartRoom_Spec.md` §5).

- `crew/` — the `ship-crew` plugin (Bridge phase 1, package 4): http hooks feeding `ship-log`'s
  changelog capture. Roles/scrutiny presets/FO orchestration land in Bridge phase 4 (package 8).

Otherwise empty for now; more plugins populate starting Chart Room phase 5 (agent surface
polish) and later Bridge/Wave 2 work.
