{
  "name": "ship-crew-plugin",
  "version": "0.2.0",
  "description": "Workspace-visible wrapper around the ship-crew Claude Code plugin payload (plugin.json, agents/, skills/, hooks/, README) -- no runtime deps, nothing to build; lint + child-process hook tests only so turbo sees it.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "echo \"ship-crew-plugin: nothing to build (plain plugin payload)\"",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@eslint/js": "^9.19.0",
    "eslint": "^9.19.0",
    "typescript-eslint": "^8.21.0",
    "vitest": "^3.0.4"
  }
}
